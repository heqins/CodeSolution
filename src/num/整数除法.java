package num;

import java.util.Objects;

public class 整数除法 {

    /**
     * 题目：输入2个int型整数，它们进行除法计算并返回商，
     * 要求不得使用乘号'*'、除号'/'及求余符号'%'。当发生溢出时，
     * 返回最大的整数值。假设除数不为0。例如，输入15和2，输出15/2的结果，即7。
     * @param args
     */
    public static void main(String[] args) {

    }

    public static int divideFunction(int a, int b) {
        if (Objects.equals(a, Integer.MIN_VALUE) && Objects.equals(b, -1)) {
            return Integer.MAX_VALUE;
        }

        int res = 0;


        return 0;
    }

    public static int divide(int dividend, int divisor) {
        int res = 0;
        while (dividend <= divisor) {
            int value = divisor;

        }

        return res;
    }
}
